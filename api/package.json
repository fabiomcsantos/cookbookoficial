{
  "name": "cookingbook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 3030"
  },
  "author": "FlyD",
  "license": "ISC",
  "dependencies": {
    "-": "^0.0.1",
    "json-server": "^0.17.3"
  }
}
